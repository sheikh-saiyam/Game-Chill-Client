import { FaStar } from "react-icons/fa";

const AllReviewCard = ({ review }) => {
  const {
    title,
    gameCover,
    reviewDescription,
    rating,
    publishingYear,
    genre,
    userEmail,
    userName,
  } = review;
  return (
    <div>

    </div>
  );
};

export default AllReviewCard;
